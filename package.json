{
  "name": "@hara-lang/hara-specs-registry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Git-authoritative Hara specification corpus and package registry.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/check-index.mjs",
    "check": "node scripts/validate-registry.mjs && node scripts/check-index.mjs",
    "test": "node --test test/*.test.mjs"
  }
}
