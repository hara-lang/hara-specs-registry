# Hara immutable artifacts

Package archives and media files are immutable SHA-256 objects. Their bytes
may be served from object storage or mirrors, but only an accepted Git manifest
connects an object to a trusted package or asset release.
