# 000004 — Immutable artifacts

The normative [`draft/hara-artifact.edn`](draft/hara-artifact.edn) defines
digest-addressed objects, manifests, provenance, protected finalization, and
registry attestations. It is distinct from the generic artifact DSL metaspec.
