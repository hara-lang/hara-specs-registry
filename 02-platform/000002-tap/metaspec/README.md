# Tap protocol meta-specification

The normative
[`hal-tap-protocol-metaspec.edn`](hal-tap-protocol-metaspec.edn) defines the
common roles, entities, operations, invariants, and conformance declarations
used by the numbered Hara tap protocol family.
