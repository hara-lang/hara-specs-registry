# XTalk substrate nodes and spaces

This draft mirrors the mutable `EventNode` and `NodeSpace` objects. It does not
add node lifecycle states, terminal close behavior, supervisors, or ownership
rules that are absent from `xt.substrate`.
