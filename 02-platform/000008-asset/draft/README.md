# Hara versioned assets

An asset collection is named and versioned like a package but belongs to a
separate catalog. Original files and CI-built derivatives are immutable
objects recorded in Git. The delivery origin has no identity credentials and
performs no arbitrary on-demand transformations.
