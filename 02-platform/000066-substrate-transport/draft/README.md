# XTalk substrate transports

This draft mirrors the function-backed `NodeTransport` and the memory, browser,
and WebSocket modules. HTA, RESP, generalized transport profiles, codec
negotiation, capability records, and lifecycle state machines are separate
concerns and are not part of this XTalk contract.
