# Hara tap identity

`id.hara-lang.org` proves GitHub ownership and publisher key possession. It
publishes signed public policy; it never receives or stores publisher private
keys. GitHub is the enrollment proof provider, while Hara Ed25519 keys
authorize releases.
