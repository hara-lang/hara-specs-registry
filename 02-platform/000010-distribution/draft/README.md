# Hara verified distribution

Distribution services are derived indexes and object origins. Clients still
verify the accepted Git record and every object. A mutable URL never replaces
an exact registry revision and SHA-256 digest.
