{:document/id :hara/std-typed-catalog
 :document/title "Portable identified schema catalogs"
 :document/summary "Immutable std.typed schema identity, exact dependency, recursive component, publication, and admission contract."
 :document/version "0-alpha"
 :document/status :draft
 :document/type :data-contract
 :catalog/format "std.typed.catalog/2"
 :catalog/hash-epoch :std.typed.schema/catalog-v2
 :catalog/component-epoch :std.typed.catalog/component-v2
 :catalog/authority
 {:normalization "hara-lang/hara:core/lib/src/std/typed/schema.hal"
  :identity "hara-lang/hara:core/lib/src/std/typed/catalog.hal"
  :publication "hara-lang/hara-specs-registry"}
 :requirements
 [{:requirement/id :std.typed.catalog/entry-envelope
   :requirement/text "An identified entry carries a qualified id, canonical SHA-256 semantic hash, portable schema form, exact coordinate, and exact direct dependencies."}
  {:requirement/id :std.typed.catalog/exact-coordinate
   :requirement/text "Execution, artifacts, and admitted dependencies use [:schema id hash] coordinates."}
  {:requirement/id :std.typed.catalog/hash-authority
   :requirement/text "The Hara std.typed.catalog hash epoch is authoritative; registry tooling must not independently reinterpret or normalize the schema grammar."}
  {:requirement/id :std.typed.catalog/component-identity
   :requirement/text "A recursive component id is SHA-256 over the portable pr-str of [:std.typed.catalog/component-v2 sorted-exact-members]."}
  {:requirement/id :std.typed.catalog/component-evidence
   :requirement/text "Published component evidence covers every entry exactly once, matches the strongly connected components of exact entry edges, and declares exact component dependencies."}
  {:requirement/id :std.typed.catalog/component-order
   :requirement/text "The component order is deterministic and dependency-first; members inside one component use exact coordinate order."}
  {:requirement/id :std.typed.catalog/atomic-admission
   :requirement/text "Malformed, duplicate, conflicting, incomplete, stale, noncanonical, or forged catalog documents fail atomically before package installation or HBC1 program release."}
  {:requirement/id :std.typed.catalog/provenance
   :requirement/text "A published fixture pins exact source repository, Git revision, source paths, catalog epochs, and registry ownership."}
  {:requirement/id :std.typed.catalog/anonymous-compatibility
   :requirement/text "Anonymous structural schemas remain valid without catalog publication or implicit global identity."}]
 :conformance
 {:fixture "conformance/catalog-v2.json"
  :validator "scripts/validate-schema-catalog.mjs"
  :native-integration "hara-lang/hara#903"}}
